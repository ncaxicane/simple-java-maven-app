pipeline {
  agent {
    node {
      any
    }
  }
  stages {
    stage('Take the grafana backup') {
      steps {
        sh 'am a file'
      }
    }
    stage('Push to the grafana-backup submodule repository') {
      steps {
        sh 'finished run'
      }
    }
  }
}
